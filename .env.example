# OpenAI Configuration
LLM_PROVIDER=openai  # or "mock" for testing without API key
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
LLM_TIMEOUT=15
LLM_MAX_TOKENS=500

# Kubernetes (for local testing)
KUBECONFIG=~/.kube/config
